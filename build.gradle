group 'com.linling'
version '1.0'

apply plugin: 'java'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    compile (
            "io.netty:netty-all:4.1.15.Final"
    )

    testCompile (
            "junit:junit:4.12"
    )
}
